class Body(object):

    def __init__(self, name, radius, mass, orbitRadius, position, velocity, acceleration):
        self.name = name
        self.radius = radius
        self.mass = mass
        self.orbitRadius = orbitRadius
        self.position = position
        self.velocity = velocity
        self.accel = acceleration
